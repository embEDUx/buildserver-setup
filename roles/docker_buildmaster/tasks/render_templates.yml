---
- name: Render Dockerfile
  template: src=make.conf.appendix.j2 dest={{ dockerfile_dir }}/files/make.conf.appendix

- name: Render Dockerfile
  template: src=Dockerfile.j2 dest={{ dockerfile_dir }}/Dockerfile
  

