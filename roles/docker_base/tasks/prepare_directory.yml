---
- name: docker_base directory
  file: path={{ base_dir }}/docker_base/ state=directory
  
- name: copy files
  synchronize: >
    src=. dest={{ base_dir }}/docker_base/
    archive=yes delete=yes
